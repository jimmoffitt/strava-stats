# Direct dependencies only — transitive packages (numpy, pillow, pytz, …)
# resolve on their own. Loose ranges so deploy hosts (e.g. Streamlit Community
# Cloud, currently on Python 3.14) can pick wheels for their Python version
# instead of building old pins from source.
streamlit>=1.50
pandas>=2.3
plotly>=5.18
matplotlib>=3.9      # legacy pipeline charts (src/publish_data.py)
requests>=2.32
python-dotenv>=1.0
kaleido>=1.3         # Plotly fig.to_image for the Export tab's PNG download
